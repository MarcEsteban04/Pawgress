# Normalize line endings to LF in the repository. Prettier and .editorconfig
# both enforce LF, so this keeps Windows checkouts from producing CRLF diffs.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
