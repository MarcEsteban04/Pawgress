import { AuthAsideResume } from "@/features/auth/components/AuthAsideResume";

/** Sign-in argues continuity rather than the product — see AuthAsideResume. */
export default function LoginAside() {
  return <AuthAsideResume />;
}
